name: Deploy Website to Pi

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: Pi
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Deploy Static Files to Nginx
        run: |
          cp -r ./* /var/www/html/
